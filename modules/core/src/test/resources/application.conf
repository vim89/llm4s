llm4s {
  llm {
    model = "openai/gpt-4o"
  }
  openai {
    apiKey = "test-key"
    # baseUrl intentionally omitted to rely on reference.conf default
  }
  tools {
    brave {
      apiKey = "test-key"
    }
  }
}

